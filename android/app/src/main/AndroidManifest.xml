<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- تنبيه التحدي اليومي. الإذن مطلوب صراحةً منذ أندرويد 13. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- لإعادة جدولة التنبيه بعد إعادة تشغيل الجهاز. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="تحدي كرة القدم"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:allowBackup="true">

        <!-- معرّف تطبيق AdMob الحقيقي (Oasis Forge · Koora Trivia).
             بناء التطوير يبقى آمناً لأنه يطلب وحدات إعلانية تجريبية
             (useTestIds في Injector). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8287765177319119~9883719679" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- مطلوب لـ share_plus حتى يعرض تطبيقات المشاركة المتاحة. -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
        <!-- مطلوب لـ url_launcher حتى يجد متصفحاً يفتح سياسة الخصوصية (أندرويد 11+). -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
